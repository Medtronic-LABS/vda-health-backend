import { MigrationInterface, QueryRunner } from 'typeorm';
export class AddMedicationsAndAdherence1723817700000 implements MigrationInterface { name = 'AddMedicationsAndAdherence1723817700000'; async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE IF NOT EXISTS "medications" ("id" uuid NOT NULL DEFAULT uuid_generate_v4(), "tenantId" uuid NOT NULL, "patientRef" varchar NOT NULL, "sourcePrescriptionId" varchar NOT NULL, "name" varchar NOT NULL, "normalizedName" varchar, "strength" varchar, "dosage" varchar, "dosageForm" varchar, "frequency" varchar, "route" varchar, "timing" varchar, "duration" varchar, "startDate" TIMESTAMP, "endDate" TIMESTAMP, "instructions" text, "status" varchar NOT NULL DEFAULT 'INACTIVE', "verificationStatus" varchar NOT NULL DEFAULT 'VALIDATION_REQUIRED', "sourceType" varchar NOT NULL, "sourceReference" varchar NOT NULL, "createdAt" TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(), "updatedAt" TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(), CONSTRAINT "PK_medications" PRIMARY KEY ("id"))`); await q.query(`CREATE TABLE IF NOT EXISTS "medication_adherence_events" ("id" uuid NOT NULL DEFAULT uuid_generate_v4(), "tenantId" uuid NOT NULL, "medicationId" uuid NOT NULL, "patientRef" varchar NOT NULL, "scheduledDate" date NOT NULL, "scheduleSlot" varchar, "status" varchar NOT NULL, "confirmedAt" TIMESTAMP WITH TIME ZONE NOT NULL, "source" varchar NOT NULL, "notes" text, "createdAt" TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(), CONSTRAINT "PK_adherence" PRIMARY KEY ("id"))`); await q.query(`CREATE INDEX IF NOT EXISTS "IDX_medications_patient_status" ON "medications" ("tenantId", "patientRef", "verificationStatus")`); await q.query(`CREATE INDEX IF NOT EXISTS "IDX_adherence_medication_schedule" ON "medication_adherence_events" ("tenantId", "medicationId", "scheduledDate", "scheduleSlot")`); } async down(q: QueryRunner): Promise<void> { await q.query('DROP TABLE IF EXISTS "medication_adherence_events"'); await q.query('DROP TABLE IF EXISTS "medications"'); } }
